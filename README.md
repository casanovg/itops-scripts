# IT operations scripts
Several scripts to run services, VirtualBox machines, backups, etc. in a bare-metal server environment based on Fedora.

$ git clone https://github.com/casanovg/itops-scripts.git
